# .deepsource.toml
version = 1

[[analyzers]]
name = python
enabled = true

[[analyzers]]
name = yaml
enabled = true

# Optional: exclude paths if needed
exclude_paths = [
    "venv/*",
    "node_modules/*",
    "build/*",
]

[autofix]
enabled = true

version = 1

# Python analyzer
[[analyzers]]
name = "python"
enabled = true

# Optional: exclude files/folders
exclude_paths = [
    "venv/*",
    "build/*",
    "__pycache__/*"
]

# Auto-fix simple issues
[autofix]
enabled = true
